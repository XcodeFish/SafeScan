{
  "name": "@safescan/core",
  "version": "0.1.0",
  "description": "SafeScan核心引擎",
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/types/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run build:types && pnpm run build:js",
    "build:js": "ts-node ../../scripts/build.ts",
    "build:types": "tsc -p tsconfig.json --emitDeclarationOnly --outDir dist/types",
    "clean": "rimraf dist",
    "test": "jest --config=jest.config.js"
  },
  "dependencies": {
    "@swc/core": "^1.3.90",
    "@swc/types": "^0.1.5"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
